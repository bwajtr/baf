/*
 * This file is generated by jOOQ.
 */
package com.wajtr.baf.db.jooq.tables


import com.wajtr.baf.db.jooq.Public
import com.wajtr.baf.db.jooq.keys.PRODUCT__PRODUCT_TENANT_ID_FKEY
import com.wajtr.baf.db.jooq.keys.TENANT_MEMBER_INVITATION__TENANT_MEMBER_INVITATION_TENANT_ID_FKEY
import com.wajtr.baf.db.jooq.keys.TENANT_MEMBER__TENANT_MEMBER_TENANT_ID_FKEY
import com.wajtr.baf.db.jooq.keys.TENANT_PKEY
import com.wajtr.baf.db.jooq.tables.Product.ProductPath
import com.wajtr.baf.db.jooq.tables.TenantMember.TenantMemberPath
import com.wajtr.baf.db.jooq.tables.TenantMemberInvitation.TenantMemberInvitationPath
import com.wajtr.baf.db.jooq.tables.records.TenantRecord

import java.time.OffsetDateTime
import java.util.UUID

import kotlin.collections.Collection
import kotlin.collections.List

import org.jooq.Check
import org.jooq.Condition
import org.jooq.Field
import org.jooq.ForeignKey
import org.jooq.InverseForeignKey
import org.jooq.Name
import org.jooq.Path
import org.jooq.PlainSQL
import org.jooq.QueryPart
import org.jooq.Record
import org.jooq.SQL
import org.jooq.Schema
import org.jooq.Select
import org.jooq.Stringly
import org.jooq.Table
import org.jooq.TableField
import org.jooq.TableOptions
import org.jooq.UniqueKey
import org.jooq.impl.DSL
import org.jooq.impl.Internal
import org.jooq.impl.SQLDataType
import org.jooq.impl.TableImpl


/**
 * Represents a tenant (company) of the application. Almost everything in
 * database is bound to a tenant. One tenant cannot see data of other tenants.
 * It's a way how to have multiple customers inside single database schema
 */
@Suppress("warnings")
open class Tenant(
    alias: Name,
    path: Table<out Record>?,
    childPath: ForeignKey<out Record, TenantRecord>?,
    parentPath: InverseForeignKey<out Record, TenantRecord>?,
    aliased: Table<TenantRecord>?,
    parameters: Array<Field<*>?>?,
    where: Condition?
): TableImpl<TenantRecord>(
    alias,
    Public.PUBLIC,
    path,
    childPath,
    parentPath,
    aliased,
    parameters,
    DSL.comment("Represents a tenant (company) of the application. Almost everything in database is bound to a tenant. One tenant cannot see data of other tenants. It's a way how to have multiple customers inside single database schema"),
    TableOptions.table(),
    where,
) {
    companion object {

        /**
         * The reference instance of <code>public.tenant</code>
         */
        val TENANT: Tenant = Tenant()
    }

    /**
     * The class holding records for this type
     */
    override fun getRecordType(): Class<TenantRecord> = TenantRecord::class.java

    /**
     * The column <code>public.tenant.id</code>.
     */
    val ID: TableField<TenantRecord, UUID?> = createField(DSL.name("id"), SQLDataType.UUID.nullable(false).defaultValue(DSL.field(DSL.raw("uuidv7()"), SQLDataType.UUID)), this, "")

    /**
     * The column <code>public.tenant.organization_name</code>. Name of the
     * tenant visible to application users within application. Modifiable by
     * tenant administrator user.
     */
    val ORGANIZATION_NAME: TableField<TenantRecord, String?> = createField(DSL.name("organization_name"), SQLDataType.CLOB.nullable(false), this, "Name of the tenant visible to application users within application. Modifiable by tenant administrator user.")

    /**
     * The column <code>public.tenant.organization_address</code>. Optional
     * tenant's company address (without country) - could be used for billing,
     * but mainly for means of postal mail contact when needed
     */
    val ORGANIZATION_ADDRESS: TableField<TenantRecord, String?> = createField(DSL.name("organization_address"), SQLDataType.CLOB, this, "Optional tenant's company address (without country) - could be used for billing, but mainly for means of postal mail contact when needed")

    /**
     * The column <code>public.tenant.organization_country_code</code>. Optional
     * tenant (or tenant's company) country of residence. Used for various legal
     * concerns. Uses ISO 3166 2-letter country code
     */
    val ORGANIZATION_COUNTRY_CODE: TableField<TenantRecord, String?> = createField(DSL.name("organization_country_code"), SQLDataType.CLOB, this, "Optional tenant (or tenant's company) country of residence. Used for various legal concerns. Uses ISO 3166 2-letter country code")

    /**
     * The column <code>public.tenant.setup_required</code>. If set to true then
     * a setup screen is displayed to the Administrator when the application is
     * run. Used for basic application setup and filling the mandatory fields.
     */
    val SETUP_REQUIRED: TableField<TenantRecord, Boolean?> = createField(DSL.name("setup_required"), SQLDataType.BOOLEAN.nullable(false).defaultValue(DSL.field(DSL.raw("true"), SQLDataType.BOOLEAN)), this, "If set to true then a setup screen is displayed to the Administrator when the application is run. Used for basic application setup and filling the mandatory fields.")

    /**
     * The column <code>public.tenant.created_at</code>.
     */
    val CREATED_AT: TableField<TenantRecord, OffsetDateTime?> = createField(DSL.name("created_at"), SQLDataType.TIMESTAMPWITHTIMEZONE(6).nullable(false).defaultValue(DSL.field(DSL.raw("CURRENT_TIMESTAMP"), SQLDataType.TIMESTAMPWITHTIMEZONE)), this, "")

    private constructor(alias: Name, aliased: Table<TenantRecord>?): this(alias, null, null, null, aliased, null, null)
    private constructor(alias: Name, aliased: Table<TenantRecord>?, parameters: Array<Field<*>?>?): this(alias, null, null, null, aliased, parameters, null)
    private constructor(alias: Name, aliased: Table<TenantRecord>?, where: Condition?): this(alias, null, null, null, aliased, null, where)

    /**
     * Create an aliased <code>public.tenant</code> table reference
     */
    constructor(alias: String): this(DSL.name(alias))

    /**
     * Create an aliased <code>public.tenant</code> table reference
     */
    constructor(alias: Name): this(alias, null)

    /**
     * Create a <code>public.tenant</code> table reference
     */
    constructor(): this(DSL.name("tenant"), null)

    constructor(path: Table<out Record>, childPath: ForeignKey<out Record, TenantRecord>?, parentPath: InverseForeignKey<out Record, TenantRecord>?): this(Internal.createPathAlias(path, childPath, parentPath), path, childPath, parentPath, TENANT, null, null)

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    open class TenantPath : Tenant, Path<TenantRecord> {
        constructor(path: Table<out Record>, childPath: ForeignKey<out Record, TenantRecord>?, parentPath: InverseForeignKey<out Record, TenantRecord>?): super(path, childPath, parentPath)
        private constructor(alias: Name, aliased: Table<TenantRecord>): super(alias, aliased)
        override fun `as`(alias: String): TenantPath = TenantPath(DSL.name(alias), this)
        override fun `as`(alias: Name): TenantPath = TenantPath(alias, this)
        override fun `as`(alias: Table<*>): TenantPath = TenantPath(alias.qualifiedName, this)
    }
    override fun getSchema(): Schema? = if (aliased()) null else Public.PUBLIC
    override fun getPrimaryKey(): UniqueKey<TenantRecord> = TENANT_PKEY

    private lateinit var _product: ProductPath

    /**
     * Get the implicit to-many join path to the <code>public.product</code>
     * table
     */
    fun product(): ProductPath {
        if (!this::_product.isInitialized)
            _product = ProductPath(this, null, PRODUCT__PRODUCT_TENANT_ID_FKEY.inverseKey)

        return _product;
    }

    val product: ProductPath
        get(): ProductPath = product()

    private lateinit var _tenantMemberInvitation: TenantMemberInvitationPath

    /**
     * Get the implicit to-many join path to the
     * <code>public.tenant_member_invitation</code> table
     */
    fun tenantMemberInvitation(): TenantMemberInvitationPath {
        if (!this::_tenantMemberInvitation.isInitialized)
            _tenantMemberInvitation = TenantMemberInvitationPath(this, null, TENANT_MEMBER_INVITATION__TENANT_MEMBER_INVITATION_TENANT_ID_FKEY.inverseKey)

        return _tenantMemberInvitation;
    }

    val tenantMemberInvitation: TenantMemberInvitationPath
        get(): TenantMemberInvitationPath = tenantMemberInvitation()

    private lateinit var _tenantMember: TenantMemberPath

    /**
     * Get the implicit to-many join path to the
     * <code>public.tenant_member</code> table
     */
    fun tenantMember(): TenantMemberPath {
        if (!this::_tenantMember.isInitialized)
            _tenantMember = TenantMemberPath(this, null, TENANT_MEMBER__TENANT_MEMBER_TENANT_ID_FKEY.inverseKey)

        return _tenantMember;
    }

    val tenantMember: TenantMemberPath
        get(): TenantMemberPath = tenantMember()
    override fun getChecks(): List<Check<TenantRecord>> = listOf(
        Internal.createCheck(this, DSL.name("tenant_check"), "(((TRIM(BOTH FROM organization_name) <> ''::text) OR setup_required))", true),
        Internal.createCheck(this, DSL.name("tenant_organization_country_code_check"), "(((char_length(organization_country_code) = 2) AND (upper(organization_country_code) = organization_country_code)))", true)
    )
    override fun `as`(alias: String): Tenant = Tenant(DSL.name(alias), this)
    override fun `as`(alias: Name): Tenant = Tenant(alias, this)
    override fun `as`(alias: Table<*>): Tenant = Tenant(alias.qualifiedName, this)

    /**
     * Rename this table
     */
    override fun rename(name: String): Tenant = Tenant(DSL.name(name), null)

    /**
     * Rename this table
     */
    override fun rename(name: Name): Tenant = Tenant(name, null)

    /**
     * Rename this table
     */
    override fun rename(name: Table<*>): Tenant = Tenant(name.qualifiedName, null)

    /**
     * Create an inline derived table from this table
     */
    override fun where(condition: Condition?): Tenant = Tenant(qualifiedName, if (aliased()) this else null, condition)

    /**
     * Create an inline derived table from this table
     */
    override fun where(conditions: Collection<Condition>): Tenant = where(DSL.and(conditions))

    /**
     * Create an inline derived table from this table
     */
    override fun where(vararg conditions: Condition?): Tenant = where(DSL.and(*conditions))

    /**
     * Create an inline derived table from this table
     */
    override fun where(condition: Field<Boolean?>?): Tenant = where(DSL.condition(condition))

    /**
     * Create an inline derived table from this table
     */
    @PlainSQL override fun where(condition: SQL): Tenant = where(DSL.condition(condition))

    /**
     * Create an inline derived table from this table
     */
    @PlainSQL override fun where(@Stringly.SQL condition: String): Tenant = where(DSL.condition(condition))

    /**
     * Create an inline derived table from this table
     */
    @PlainSQL override fun where(@Stringly.SQL condition: String, vararg binds: Any?): Tenant = where(DSL.condition(condition, *binds))

    /**
     * Create an inline derived table from this table
     */
    @PlainSQL override fun where(@Stringly.SQL condition: String, vararg parts: QueryPart): Tenant = where(DSL.condition(condition, *parts))

    /**
     * Create an inline derived table from this table
     */
    override fun whereExists(select: Select<*>): Tenant = where(DSL.exists(select))

    /**
     * Create an inline derived table from this table
     */
    override fun whereNotExists(select: Select<*>): Tenant = where(DSL.notExists(select))
}
